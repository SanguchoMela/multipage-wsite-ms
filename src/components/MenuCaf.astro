---
import { Image } from "astro:assets";
import { menuItems } from "../data/menu";
---

<section class="mt-12">
  <div class="space-y-4">
    <h1>Viaje de Sabores</h1>
    <h3>"Un mundo de sabores en una sola visita."</h3>
    <p>
      Lorem ipsum, dolor sit amet consectetur adipisicing elit. Ratione
      voluptate temporibus voluptatibus dicta quidem, dignissimos totam eaque
      excepturi corrupti? Officiis, ipsam. Sequi consequuntur unde nihil aliquam
      placeat quos suscipit numquam.
    </p>
  </div>
</section>
<section class="bg-amber-50 space-y-4">
  <div class="space-y-4">
    {
      menuItems.map((section) => (
        <Fragment>
          <hr class="text-amber-900" />
          <div class="flex gap-10 my-6 lg:my-10 flex-wrap justify-evenly">
            <div class="flex items-center justify-center w-full">
              <h3 class="text-2xl font-semibold">{section.category}</h3>
            </div>
            {section.items.map((item, index) => (
              <div class="bg-white w-72 lg:w-96 rounded-lg shadow-md hover:shadow-lg transition grid grid-cols-1 lg:grid-cols-2">
                <div class="overflow-hidden h-32 md:h-40 lg:h-56 w-full flex justify-center items-center rounded-t-lg lg:rounded-l-lg rounded-lg-none lg:rounded-t-none">
                  <Image
                    src={item.img}
                    alt={`Menú item ${index + 1}`}
                    class="h-full w-full object-cover"
                  />
                </div>
                <div class="flex flex-col justify-evenly p-2 md:p-4 max-h-56 space-y-1">
                  <h4 class="text-lg font-semibold">{item.name}</h4>
                  <p class="text-sm text-slate-600">{item.description}</p>
                  <span class="font-medium text-slate-700">{item.price}</span>
                </div>
              </div>
            ))}
          </div>
          <hr class="text-amber-900" />
        </Fragment>
      ))
    }
  </div>
</section>
